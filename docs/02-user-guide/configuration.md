# Configuration

File: `config/laravel-notifications.php`.

- `default_channels` — used when `Notification::send()` omits the channels argument
- `channels.telegram|slack|mail` — credentials and timeouts

See the root README environment table for env names.
